spring:
  application:
    name: quizcard-api
  datasource:
    url: jdbc:postgresql://localhost:5432/quizcard_api_db
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2